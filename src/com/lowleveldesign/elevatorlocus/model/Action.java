package com.lowleveldesign.elevatorlocus.model;

/**
 * The action a passenger performs when the elevator stops at a floor.
 */
public enum Action {

    /** Passenger boards the elevator (a pickup). */
    IN,

    /** Passenger leaves the elevator (a drop-off). */
    OUT
}
